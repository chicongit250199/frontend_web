import { CardFormatPipe } from './card-format.pipe';

describe('CardFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CardFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
